# HLDspec Workspace — Claude Launch Shim

Read and follow `AGENTS.md` in this workspace before taking any action.

`AGENTS.md` is the universal source of truth for HLDspec agent behavior across
Claude, Codex, and Devin. This file exists only so Claude-specific tooling has a
conventional entry point. If this file conflicts with `AGENTS.md`, follow
`AGENTS.md`.

After reading `AGENTS.md`, run the bootstrap command documented there.
