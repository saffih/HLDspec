# HLDspec Test Strategy V2

V2 uses active contract and machine tests under `tests_v2/`.

Legacy tests are preserved under `tests_legacy/` and are not active during the V2 rewrite.

Do not delete legacy tests blindly.

Delete a legacy test only when a stronger V2 contract or behavior test replaces it.
